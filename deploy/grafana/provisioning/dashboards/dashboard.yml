# Grafana 自动加载看板（从 dashboards/ 目录）
apiVersion: 1

providers:
  - name: "gov-agent"
    orgId: 1
    folder: "Gov Agent"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
