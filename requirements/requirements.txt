# =====================================================
# requirements/requirements.txt - 开发/测试全量依赖
# Python >= 3.12
#
# Core:
# FastAPI + LangGraph + MCP + RAG + AgentOps
#
# 运行时依赖在 runtime.txt（API/MCP 镜像只装它，见 deploy/Dockerfile*）；
# 本文件在 runtime 之上叠加测试/前端/代码质量依赖，供本地开发与 CI。
# =====================================================

-r runtime.txt


# =====================================================
# Testing
# =====================================================

pytest>=8.3.4,<9.0

pytest-asyncio>=0.25.0,<1.0

pytest-cov>=6.0.0,<7.0


# =====================================================
# Frontend
# =====================================================

streamlit>=1.40.0,<2.0


# =====================================================
# Code Quality
# =====================================================

ruff>=0.9.0,<1.0

black>=25.0.0,<26.0

mypy>=1.14.0,<2.0
