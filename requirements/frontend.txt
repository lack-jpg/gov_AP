# =====================================================
# requirements/frontend.txt - Streamlit 前端运行时依赖
# 供 deploy/Dockerfile.frontend 使用（轻量，避免引入 backend 重型依赖）。
# =====================================================

streamlit>=1.40.0,<2.0

httpx>=0.28.0,<1.0
