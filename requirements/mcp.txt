# =====================================================
# requirements/mcp.txt - MCP Gateway + 3 Servers 运行时依赖
# 供 deploy/Dockerfile.mcp 使用。
# 注意：workflow_server 经 database.case_service 落库（PostgreSQL），
#       需 sqlalchemy + asyncpg；gateway 校验 JWT 需 python-jose。
# =====================================================

fastapi>=0.115.6,<1.0

uvicorn[standard]>=0.34.0,<1.0

pydantic>=2.10.0,<3.0

pydantic-settings>=2.7.0,<3.0

httpx>=0.28.0,<1.0

loguru>=0.7.3,<1.0

python-jose[cryptography]>=3.3.0,<4.0

sqlalchemy>=2.0.36,<3.0

asyncpg>=0.30.0,<1.0
