# =====================================================
# requirements/mcp.txt - MCP Gateway + 3 Servers 运行时依赖
# 供 deploy/Dockerfile.mcp 使用。
# 注意：workflow_server 经 database.case_service 落库（PostgreSQL），
#       需 sqlalchemy + asyncpg；database 包 __init__ 顶层导入 database.redis
#       （import redis.asyncio），缺 redis 会 ModuleNotFoundError → 办件落库失败；
#       gateway 校验 JWT 需 python-jose。
# =====================================================

fastapi>=0.115.6,<1.0

uvicorn[standard]>=0.34.0,<1.0

pydantic>=2.10.0,<3.0

pydantic-settings>=2.7.0,<3.0

httpx>=0.28.0,<1.0

loguru>=0.7.3,<1.0

python-jose[cryptography]>=3.3.0,<4.0

sqlalchemy>=2.0.36,<3.0

asyncpg>=0.30.0,<1.0

redis>=5.2.0,<6.0

# =====================================================
# RAG 检索（P1-1 真实检索主链路）
# policy_server 的 search_policy → rag.pipeline：
#   Embedding(BGE/sentence-transformers) → Milvus 稠密 + BM25 稀疏 → BGE Reranker
# 模型文件经 compose 只读挂载 ./models:/app/models，语料挂载 ./data:/app/data。
# =====================================================

numpy>=2.0.0,<3.0

jieba>=0.42.1

pymilvus>=2.5.0,<3.0

transformers>=4.48.0,<5.0

sentence-transformers>=3.3.0,<4.0

FlagEmbedding>=1.3.0,<2.0
