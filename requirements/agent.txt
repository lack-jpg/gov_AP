# =====================================================
# requirements/agent.txt - A2A Mock Agents 运行时依赖
# 供 deploy/Dockerfile.agent 使用（housing + fund mock server）。
# =====================================================

fastapi>=0.115.6,<1.0

uvicorn[standard]>=0.34.0,<1.0

pydantic>=2.10.0,<3.0

pydantic-settings>=2.7.0,<3.0

httpx>=0.28.0,<1.0

loguru>=0.7.3,<1.0
